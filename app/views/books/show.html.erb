<%= render 'userinfo', user: @book.user, book: @book.user %>

<h1>Book detail</h1>

  <p><%= image_tag @book.user.get_profile_image(100,100) %></p>
  <p><%= link_to @book.user.name,user_path(@user.id) %></p>
  <p><%= link_to @book.title,book_path(@book.id) %></p>
  <p><%= @book.body %></p>

